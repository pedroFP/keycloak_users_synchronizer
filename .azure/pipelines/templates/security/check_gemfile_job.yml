jobs:
- job: check_gemfile
  displayName: Check Gemfile
  dependsOn: start_containers
  continueOnError: true
  steps:
    - script: |
        docker compose -p $(APP_NAME)_$(CI_COMMIT_SHORT_SHA) exec keycloak-users-synchronizer bundle exec bundle outdated --strict
      displayName: List outdated gems
